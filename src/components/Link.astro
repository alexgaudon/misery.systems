---
interface Props {
  href: string;
  class?: string;
  brackets?: boolean;
}

const { href, class: className = '', brackets = true } = Astro.props;
---

<a href={href} class:list={['terminal-link', className]} data-brackets={brackets.toString()}>
  <slot />
</a>
